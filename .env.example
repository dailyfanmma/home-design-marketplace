# SQLite is fine for local dev; swap to a Postgres URL for production.
DATABASE_URL="file:./dev.db"

# Flat percentage the platform keeps from every awarded design fee (0-1).
PLATFORM_FEE_PCT="0.15"

# Not wired up yet -- placeholders for when real payments/auth replace the
# mock login and the stubbed booking flow. See README "Next steps".
# STRIPE_SECRET_KEY=
# STRIPE_CONNECT_CLIENT_ID=
# NEXTAUTH_SECRET=
